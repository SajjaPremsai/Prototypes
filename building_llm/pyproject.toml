[project]
name = "building-llm"
version = "0.1.0"
description = ""
authors = [
    {name = "Sajja Premsai",email = "sajjapremsai8938@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
    "tiktoken (>=0.13.0,<0.14.0)",
    "torch (>=2.12.0,<3.0.0)"
]

[tool.poetry]
packages = [{include = "building_llm", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
